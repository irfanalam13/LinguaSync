# Voice_backend — API gateway ONLY. No ML libraries here by design.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.9
pydantic-settings>=2.5
email-validator>=2.0      # pydantic EmailStr
httpx>=0.27               # client to the Voice_ML service + TestClient

# Phase 5 platform
sqlalchemy>=2.0
alembic>=1.13
bcrypt>=4.0
pyjwt>=2.9
psycopg[binary]>=3.2      # Postgres driver (Neon/Render); URL auto-normalized to +psycopg
redis>=5.0               # queue (rq backend)
rq>=1.16
minio>=7.2               # object storage (minio backend)
# tests
pytest>=8.0
pytest-asyncio>=0.23
