# Voice_backend — API gateway ONLY. No ML libraries here by design.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27          # client to the Voice_ML service + TestClient

# tests
pytest>=8.0
pytest-asyncio>=0.23
