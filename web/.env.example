# Same-origin by default (Next rewrites /api/* → BACKEND_URL). Override for split deploys.
BACKEND_URL=http://127.0.0.1:8000
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
# NEXT_PUBLIC_WS_URL=https://api.your-domain.com
